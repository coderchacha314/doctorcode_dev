# DoctorCode — Development Environment Defaults
# These are non-secret defaults for the dev environment.
# Actual secret values must be set in .env.local (gitignored).
# This file IS committed to git — do not add real secrets here.

NEXT_PUBLIC_APP_URL=https://dev.doctorcode.app
NODE_ENV=development
RESEND_FROM_EMAIL=no-reply@dev.doctorcode.app
CLOUDINARY_UPLOAD_PRESET=doctorcode_profiles
